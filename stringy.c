#include <stdio.h>
#include <stdlib.h>

//strlen:returns the number of characters form the sttar of s up to
//but not including the null
int my_strlen(char *s){
  int x = 0;
  int len = 0;
  while(s1[x] != 0){
    len++;
    x++;
  }
  return len;
}

//strcpy: (char *dest, char *source) copies all the characters up to 
//and including the null returns dest
char my_strcpy(char *destination, char *source){
}

//strncat:(char *s1, char *s2) appends the characters up to 
//and indulding the terminating null in s2 at the end of s1
//returns s1
char my_strncat(char *s1, char *s2){


  return s1;
}

//strcmp: returns 0 if s1 and s2 are identical
//postitive if more, negative if less
int my_strcmp(char *s1, char *s2){
}

//strchr:
my_strchr(){
}
//strstr:
my_strstr(){
}
